# 结论寄存器（跨会话防绕圈用）

> 2026-09-23 建。**这是给长任务用的"短到能整篇读完"的结论表**——`docs/experiment_log.md` 是日志
> （越长越读不完），本文件只放**已定论**的东西，一行一条。
> **每轮开始先读本文件**；**每轮结束必须新增或更新至少一行**；**想重开一个"已排除"的方向，
> 必须先写明"为什么这次的证据会与当初不同"**（规则见 `docs/goal_longrun_plan.md` §4）。

## 当前方针（三行，随时更新）

- **在攻**：方法层假设 H1–H4（`goal_longrun_plan.md` §2.1）。短期目标由自己提，但**先写假设 + 先写判据**。
- **为什么**：`B0_step0_smoke` 显示我们在实战里几乎不建经济（368 回合只出招 16 次、建塔 6 座，对手 151 次/76 座）。
  **按用户定的立场，这是"训练方法学不会"，不是"要给它加建塔先验"** ⇒ 观测"建塔数"当**证据**，不当目标。
- **下一步**：填 B 基线（`B1/B2` 跑中）→ 在钉死协议下重测 `vs rule_v4` 当前基线 → 挑一个假设做便宜实验。

## 怎么读这张表（**它不是权威；允许、而且鼓励被推翻**）

1. **每条都是"当前最佳解释"，不是事实。** 尤其"解读"部分：同一组读数可以有不同解释，
   而解释会受**当时提问方式、已有信念、以及我的主观性**影响。**明确把"读数"和"对读数的解读"分开**：
   读数（命令 + 输出）通常可复核；**解读是判断，推翻时多半推翻的是它**。
2. **实验有随机性** ⇒ 看 **n / 是否配对镜像 / SE**。**n 小或单次的一律标"弱"**，
   不许当成定论用（项目史上"单臂排名在轮次间完全不可复现"）。
3. **允许推翻，而且推翻不是异常操作。** 本文件的历史里就发生过：同一天我对"对局是否确定"
   连翻两次结论（先判"CPU 争抢"、再判"确定"、最后实测为"不可复现"）。**推翻要照同一条纪律走**：
   写清原结论的**可能失效点** + 用预注册判据重做 + 记档。
4. **"已排除"的正确读法**不是"永远排除"，而是"**在没有新证据或新理由之前，不值得再试**"。
   所以重开它的门槛是两问：① 当初那条判据**哪里可能失效**？② 这次的做法**哪里不同**？
5. ⚠️ **强度标记的含义**（每行必标）：

   | 标记 | 含义 |
   |---|---|
   | **强** | 多套独立读数方向一致，或用**逐字段级**比对、可复核 |
   | **中** | 单批内聚合、配对，n 中等；方向可信但幅度不可靠 |
   | **弱** | 单次 / 小 n / 未配对 / 只是机制读数 ⇒ **不能当定论** |
   | **存疑** | 已有反面证据，或曾被推翻过一次 |

## 已证实

| 结论 | 支撑（run / 文件）| 强度 |
|---|---|---|
| 我们的 C++ 引擎 **== 官方二进制**（精确字段逐回合一致）| `test_official_consistency.py --rounds 30`；`test_official_ops.py` 14/14 | **强**（两套独立测试，逐字段）|
| `resolve_turn` ≡ `apply_operation_list`+`advance_round` | 三路径对照 P2=P3 8/8 | **强**（逐字段）|
| **协议层不改变对局**（进程内直连 vs 协议 bridge 等价）| `ctrl_rv4_self_inprocess` vs `A1_rv4_self` 16/16 逐字段一致 | **强**（逐字段）|
| `rule_v4` **确定性**（镜像两局读数逐字段相同）| `A1_rv4_self` 8/8 对 | **强** |
| **分帧 bug（已修）**：bridge 的 `strip_cr` 曾在读取层删流里的 `0x0D` 字节，而这条流带**二进制长度前缀** ⇒ 某次回包 payload 长度含 0x0D 时（13、269…）整条流错位、这局被超时掐断 | **hex dump 逐字节吻合**（payload 13 字节、误读长度 50=`0x32`='2'）+ `_tmp_test_framing.py` PASS | **强** |
| **冠军/亚军不可复现** ⇒ **只读聚合、不读逐 seed** | 四跑（`DET*`/`DET3_seed11_hexdump`）| **存疑**（⚠️ **证据被分帧 bug 污染**：三次"卡在 r84"已确认就是那个 bug（`need 50B, have 12B` 同签名），不是 AI 的问题；但"第 84 回合棋盘不同"这一点仍未解释 ⇒ **必须重测**才能恢复/推翻）|
| `rule_v4` vs 冠军 **0/16**、vs 亚军 **0/16**，且**被打爆基地** | `A2a` / `A2b` | **中**（配对、n=16/对手；"0"≠"恰为 0"，上界 ~20%）|
| 历史读数**全部**在 C++ 引擎上（内部自洽）| `_tmp_audit_eval_cmds.py`：14/14、6/6 带 flag；两工具默认 True | **中**（只覆盖**进了日志**的 run）|
| 判词是**官方 5 级级联**；官方**从不判平局**；异常截断局须标 `INVALID` | `game.cpp: judge_winner()`；bridge RESULT（commit `57724c0`）| **强**（读源码 + 实测一致）|
| 我们的协议壳在**长局可靠**：367 回合 `mismatch=0`、`ill=0` | `B0_step0_smoke`（**单局**）| **弱**（单次；但逐回合对拍是硬读数）|
| `main.exe` **不是**官方预编译产物，是我们自己编的 | 子模块 `.gitignore:38`；仅 2 处改动 | **强** |

## 已排除（**在没有新证据/新理由之前，别再试**）

| 结论 / 排除的理由 | 支撑 | 强度 |
|---|---|---|
| **用 Python SDK 引擎测强度**（它漂移：接受官方拒绝的操作）| `test_consistency.py --rounds 120` → 0/6 seeds 一致 | **中**（6 seed；但错法清楚、可复核）|
| **跨引擎比较**（同 seed 只换引擎，`rule_v4` 自对弈 4/8 胜负翻转）| 三路径对照 P1 vs P2/P3 | **中**（8 seed，逐字段）|
| **单次 / 单 seed / 单臂读数下结论** | 项目史（单臂排名不可复现）+ `DET*` 四跑 | **强**（多次独立发生）|
| **用短回合上限判断"AI 是否弃权"**（如 8 回合）| 08-09 错误结论；`rule_v4` 前 ~30 回合本来攒钱（首次出招 r42）| **强** |
| **追"引擎版本差异"的根因**（三份源码逐字节相同；Python 才是漂的那边）| 源码比对 + `test_consistency.py` | **中** |
| **把"建经济"当修复方向硬修**（症状，非病因）| 用户立场（`goal_longrun_plan.md` §2）| **中**（是**立场**，不是实验结论）|

## 待验证

| 问题 | 计划 / 状态 |
|---|---|
| **我们 vs 冠军 / vs 亚军** 的基线（多少 / N）| `B1_us_vs_champion`、`B2_us_vs_runnerup`（跑中）|
| **我们 vs `rule_v4` 在当前钉死协议下的基线**（历史 46.9% 是旧协议）| 待跑（rule_v4 一局 1.5–2 分钟）|
| `rule_v4` 每局**建塔数**（机制基线）| 待跑 |
| 方法层假设 **H1**（信用分配 / 96% HOLD 标签不平衡）| 待设计便宜判据 |
| 方法层假设 **H2**（价值标签视野 `tau`）| 已有先例 tau20→50 让 search-vs-search 43.75%→81.2% |
| 方法层假设 **H3**（策略表达不出多头 bundle）| 待设计 |
| 方法层假设 **H4**（类轴 masked/HOLD 结构）| 待设计 |
| **两体架构（A 监督 / B 执行）是否有效** | 计划：`docs/ab_supervisor_plan.md`（**未做任何改动**；先做该文档 §6 的 3–5 轮小规模试验，失败判据=只会复述 B）|
