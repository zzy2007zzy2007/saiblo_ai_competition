# 磁盘占用盘点与可清理候选（2026-09-16）

D: 共 694 GB，已用 **644 GB，仅剩 51 GB**。`training_history/` ≈ **390 GB**（占六成）。

## 关键结论：**按体积算，几乎没有"珍贵"的东西**

| 必须保留 | 体积 |
|---|---|
| **全部 AZ 时代的 `.pt`**（`az_fixed` / `az_cpp` / `az_intent` / `az_loop` / `az_bn_kgeo`）| **1.4 GB**（约 380 个，单个 ~3 MB） |
| `training_history/runs/`（run_logged 的命令/日志/meta）| 3.1 MB |
| `docs/` | 0.76 MB |

⇒ 空间**全部是数据**，而且要么属于**已废弃的线**，要么**可重生成**。
另外：**没有任何单文件超过 420 MB**，所以不是几个巨型文件的问题，是几千个小文件堆起来的。

## 最大的 .pt 是"种群快照"，不是 checkpoint

`training_history` 里 1,447 个 `.pt` 共 208 GB，但**几乎全在 ES/GA 与 PPO 时代**：

| 时代 | `.pt` 体积 | 文件数 | 是什么 |
|---|---|---|---|
| `ga_ss_*`（7/23–7/30）| **172 GB** | 414 | ES/GA 的**每代种群快照** `gen_XXXX.pt`（~400 MB/个） |
| `ss_20260709/10` | 12 GB | ~120 | 最早的自对弈/策略空间阶段 |
| `ppo_*`（8/2–8/4）| 23 GB | 485 | PPO 时代的中间模型 |
| **AZ 时代（8/6 起）** | **1.4 GB** | ~380 | **真正在用的 checkpoint（~3 MB/个）** |

## 分档：可清理候选

### P1 —— 已废弃的线 + 我自己造出的冗余（约 267 GB）

| 目录 | 体积 | 说明 |
|---|---|---|
| `ga_ss_2026*`（20 个）| **≈206 GB** | ES/GA 时代（7/23–7/30），**已废弃**；其中 172 GB 是每代种群 `.pt` |
| `ppo_*`（约 70 个）| **≈31 GB** | PPO 时代（8/2–8/4）的实验目录，**已废弃** |
| `ss_20260709_*` / `ss_20260710_155327` | **≈11 GB** | 最早的自对弈阶段（7/9–7/10） |
| `value_2026071x_*` / `score_distill_*` | <0.2 GB | 7 月的价值/蒸馏实验 |
| **`posnet_p2/raw`** | **15 GB** | **今天 Phase 2 的原始 pkl**；已过滤成 `compact/`（440 MB）供训练用，raw 可丢（36 分钟可重采）|
| `az_loop/round1..4` | **7.2 GB** | 价值迭代循环的各轮原始数据；**是 `az_loop/pool` 的子集**（pool 是 merge 后的），重复 |

### P2 —— 可重生成的数据池（约 95 GB，建议**移到硬盘**而不是删）

| 目录 | 体积 | 重生成代价 |
|---|---|---|
| `az_cpp/data` | **43 GB** | C++ 引擎自对弈数据（batch 30+ 的价值池来源），重采约 10h+ |
| `az_fixed/{_v50pool,_polpool,data,warm_combined,_v50pool336,data_polonly}` | **≈36 GB** | 各池/暖启动数据 |
| `az_intent/az_selfplay_data` | 9.8 GB | 自对弈数据 |
| `az_bn_kgeo/data` | 6.0 GB | BN+kgeo 实验数据 |
| `az_intent/warm_data*` | 4.3 GB | 暖启动数据集（可由 ckpt 重新前向生成）|

### P3 —— 别动

所有 `az_*` 里的 `.pt`（1.4 GB，当前线的 checkpoint）、`runs/`、`docs/`、以及 `code/`、`Ant-Game/`。

## 执行建议

1. **先移/删 P1 的 ES/GA + PPO（约 250 GB）** —— 这是回本最快的一笔，且与当前 AZ 线完全无关。
2. `posnet_p2/raw`（15 GB）与 `az_loop/round1..4`（7.2 GB）是**纯冗余**，可以直接删（我都不需要它们就能重跑）。
3. P2 建议**挪到外部盘**保留，因为它们是当前线可用的价值池；删了要想回来得花几小时到十几小时重采。
4. 移完之后 D: 大约能回到 **300 GB 以上可用**。
