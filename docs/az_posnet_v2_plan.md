# 计划：位置网第二版 —— 多类记录 + 两段式目标（1-ply 价值 → 搜索的 visit）

> 日期：2026-09-20（20:xx 修订：把"随机钉类"换成"**多类记录**"，用户提出）。
> 状态：**计划阶段，待过目。**
> 前置：`az_value_prior_plan.md` §0.3.1（"目标要更深"的证据）、`az_class_axis_plan.md`
> §5(a)（价值网刚被改进）/§5(c)、`az_posnet_random_class_plan.md`（钉类机制的定义）。

## 1. 事实基础（2026-09-20 全部复核过，不是记忆）

**1.1 现在的 `posnet_r1` 学的是「1-ply 价值偏好」，不是 MCTS visit。**
`collect_value_prior.py:86` 是 `c = int(np.argmax(hl[h]))`（注释明写"与 `--search-mode pos-only`
一致"），逐 (决策点, 头) 记录 `(board, stats, player, head, cls, cell, adv)`，其中
`adv` = **价值网**在每个候选 post-state 上的打分；`train_value_prior.py` 的目标是
`p = softmax(z(adv)/target_tau)`，预测 `q = softmax(z(action_map[cls])/t_pos)`。

**1.2 用 1-ply 而不是 visit 的原因是成本。** `az_value_prior_plan.md` §2 原话：visit 目标要
k=200/it=8192 才稳定，而当时测得"visit 的排序主要由价值驱动 ⇒ 高 k/it 的 visit 目标
≈ 价值 1-ply 的 softmax 目标" ⇒ 拿 1-ply 当**便宜的代理**。

**1.3 现在的数据只用到局面的极小一部分（两个"1"）。**
- **只记 1 个类**：`for h in range(3): c = argmax(...)`，每个 (回合, 头) 只记录 argmax 类的行。
  直接数 `training_history/vprior/vp_400.npz`：**24,972 行，`cls` 全是 17**，涉及不同类数 = 1。
- **只在"该类可执行且有 ≥2 格"时才记**：所以连这一类的行都只覆盖"闪电买得起"的那些回合。
- 而**同一局面里其实有 ~19 个类可用**（我实测 2 局、逐 (回合,头) 数
  `{c != HOLD : class_mask[c] 且 position_mask[c].any()}`）：
  **均值 18.9 / 中位 19 / 最大 20（共 23 类）**。
  ⇒ **现状把每回合的信息用了约 1/19（按类算）、且只用了 ~1/9 的回合（按"可记回合"算）。**

**1.4 🔴 它模仿的那个目标本身是弱点。** `az_value_prior_plan.md` §0.3.1 的分布外检验
（**只换闪电落点**、打完整 rule_v4）：

| 落点来源 | rule_v4 规则 | 我们的网 | 1-ply 价值 | **256 迭代搜索** |
|---|---|---|---|---|
| 胜率 | 0.5000 | **0.4922** | 0.3281 | **0.5391** |
| 只放闪电（赢局数 /128）| 22 | 25 | 23 | **43** |

⇒ **搜索（访问平均）的选点远好于任何"一次性"选点**，包括我们的位置网**和它模仿的那个目标**。
位置网作为"1-ply 目标"的拟合器已经满分（learned ≈ oracle），但**那个目标不是搜索手里的知识**。

**1.5 价值网刚被改进（今天）。** 只换 `value_state`（注入数据微调，权重相对变化 4.1%），
vs rule_v4 **18.4% → 32.4%（+14.1pp，McNemar p=0.0003）**。因为 1-ply 目标是**从价值网导出的**
⇒ **第一段的教学信号现在本身就变好了**，而采集成本不变。

**1.6 ✅ 已解答：网（0.4922）比它模仿的目标的 argmax（0.3281）好，是因为"硬取 max 被 winner's curse 带偏"。**

1. **不是配置不对称**（用户提出的可能解释）：核了当时的脚本 `_tmp_rv4_lightning.sh`
   与实现 `其他版本ai/rule_v4_mcts_lightning/mcts_lightning.py:122-160` ——
   `net` 臂 = `action_map[17]` 在合法格上取 argmax（**一次前向、无搜索**），
   `value` 臂 = 逐格建 post-state 过价值网取最大（**无搜索**），只有 `mcts` 臂跑 256/4 搜索
   （且它是另一次 run）。三个臂的价值网也是同一个。⇒ **疑点是真疑点。**
2. **机制**：`value` 臂在 **~271 个带噪的 1-ply 估计上取最大值** ⇒ 挑的是"**估计值**最高"的格，
   噪声让这个选择系统性偏乐观（winner's curse）。而位置网是对 `softmax(z(adv)/τ)` 的**平滑拟合**，
   不做硬 max ⇒ 避开离群格。
   **实测支持**（`vp_400.npz` 24,972 个决策点跑 `posnet_r1` 的 `pos_state` 前向）：
   **网的 argmax 与 1-ply 价值 argmax 只有 39.0% 同格（61.0% 不同）** ⇒ 网**不是**忠实复制
   目标的 argmax。这与"τ=1 让目标接近平坦 ⇒ CE 由分布主体主导 ⇒ 网学的是平滑结构"一致。
   ⇒ **不要为了"更贴近目标"去调小 τ**（那会去拟合噪声峰）；`target_tau = t_pos = 1.0` 是对的。

**1.7 ⚠️ intent decoding 带来的一个坑（本次修订才发现，必须显式处理）。**

"哪些类这回合可用"**不需要手推**：`make_class_mask` / `make_position_masks(intent_decoding=True)`
已经算好了（随机钉类抽的就是这个集合）。但要注意三件事：

1. **"可用"≠"它的通道会被读"**。`intent_decoding=True` 下**买不起的超武类仍在 mask 里**
   （实测：coins=50 时 `pm[17]` 仍有 271 格、`cm[17]=True`）。
2. **买不起的超武会被解码成"降级"，而且读的是通道 16 的图**。于是收集器"把某格强行顶到最高"
   这个手法**失效** ⇒ 若此时通道 16 有格（= 你有塔），**每个格都会返回同一个降级操作**
   ⇒ 该行的 `adv` 会**全相等（退化/错标）**。
3. **现有数据没有这种坏行是侥幸**：旧对局**从来没有塔** ⇒ `pm[16]` 恒空 ⇒ 自动降级找不到位置
   ⇒ `decode_head` 对每个格都返回 `None` ⇒ 整行被 `if op is None: continue` 丢掉。
   我实测 `vp_400.npz` 的 24,972 行**没有一行退化**（adv 标准差 0 的行 = 0%，271 个值全不同）。
   ⇒ **新设计一定会遇到塔，所以必须显式校验。**

⇒ **强制要求（写进实现）**：记录一行之前，校验 `decode_head` 返回的操作**真的落在被顶起来的
那一格上**（`op.arg0/arg1 == (x, y)`），否则丢弃该行。这一条同时封掉"自动降级错标"和其他
任何"顶格没生效"的情形，并**打印被丢弃的行数**。

## 2. 两段式目标（核心主张）

| 段 | 目标 | 成本 | 覆盖通道 |
|---|---|---|---|
| **第 1 段** | 1-ply 价值偏好 `softmax(z(adv)/τ)` | 中（**不需要搜索**，只要价值网前向）| **全部可用的类**（靠 §3 的多类记录）|
| **第 2 段** | **MCTS visit**（钉类 + 只搜位置，标准 AlphaZero 的位置分支）| 高（要搜索）| 每次钉 1 个通道（按 §4.4 轮换）|

**为什么两段而不是直接上第 2 段**：① 第 2 段的 visit 目标在**尖通道**上会退化（候选数 1 ⇒ 无信息）
且访问次数少、噪声大（§4.2）；第 1 段的目标是**状态的确定性函数**（平滑），适合先把地图的大形状学出来
（**注意**：§4.2 实测候选数主要取决于"该类这回合**可不可执行**"，与"通道尖不尖"无关；
可执行的回合上连闪电都有 24 个候选）。
② 第 1 段便宜得多 ⇒ 可以先把所有通道都铺上监督。③ 第 1 段的原料（价值网）刚变好。

## 3. 第一步（本计划要实现的）：多类记录 + 按通道加权（**行为不变**）

**3.1 机制改动（只有采集端，训练端不动）**

把 `collect_value_prior.py` 的"每个头只记 argmax 那一类"换成**记录一批合法的类**：

```
可用类集合 = { c : c != HOLD, class_mask[c], len(position_mask[c]) >= 2 }      # 用 mask 推，不手写规则
记录的类   = argmax 类  ∪  从其余可用类里均匀抽 (K-1) 个                        # K 为旋钮
每类记录的格 = 从该类的合法格里取 min(M, 全部) 个                              # M 为旋钮
```

- **总是包含 argmax 类**（它是部署时唯一真正被用的那一个 ⇒ 不能让它缺数据）。
- **其余类随机抽**（而不是固定取前几个）⇒ 跨回合覆盖所有通道，且每个通道拿到的局面多样。
- **每类最多 M 格**：目标仍是"这个子集上的 softmax 偏好"（语义不变——部署时解码器也只是在
  这一类的合法格里挑），但成本可控。
- 训练端（`train_value_prior.py`）不变：仍逐行做位置 CE；现在会有多个 `cls` 值，
  **自动就训到了所有被记录过的通道**。

**3.2 成本（实测，必须知道）**：不做任何限制地"记全部合法类 × 全部格"的代价是

| | 现状（只记 argmax 类）| 记全部合法类（无上限）| 倍数 |
|---|---|---|---|
| 目标行数 | 414 | 80,196 | **193.7×** |
| 价值网前向（格数）| 112,194 | 5,773,788 | **51.5×** |

（2 局实测。）⇒ **不能裸做**，必须靠 M/K 压回。**实测 `K=5, M=32`（2026-09-20 冒烟，2 局）**：

| | K=1, M=0（现状）| **K=5, M=32** | 倍数 |
|---|---|---|---|
| 行数/局 | 60.8 | **879.5** | **14.5×** |
| 价值网前向/局 | 16,463 | **28,144** | **1.71×** |
| 覆盖的类 | **1** | **20** | — |
| 守卫丢弃（顶格/退化）| — | **0 / 0** | — |

⇒ 比预估的 5~6× 还便宜（M 封顶省下的抵消了 5 倍的类数）。**成本不是瓶颈。**

**3.3 ⚠️ 按通道加权（否则会把闪电通道训坏）**：现状闪电占 **100%** 的行；多类记录后
**实测掉到 6.7%**（20 个类，最高的是建塔 7.1%）。而闪电是**部署时唯一被使用的通道** ⇒ 必须加权：

```
行权重 = 1.0   （argmax 类，= 部署时真正会用的那类）
       = λ     （其余类；λ 为待标定旋钮，先取 0.2）
```

⚠️ **实测发现一个我原先没想到的细节**：argmax 类的行**只占 6.7%，而不是"1/K = 20%"**。
原因：`picks` 里只有 `c_arg` **可用**时才把它算进去，而闪电大多数回合不可执行
（买不起/冷却中，见 §4.2）⇒ 那些回合的 K 个名额全给了非 argmax 类。
⇒ **加权不只是"按类分权"，它同时改变了总权重预算**：K=1 时每行权重 1、行数少；
K=5 时行数 14.5×、但其中 93% 的行只有 λ=0.2 ⇒ **平均行权重 ≈ 0.26**。
所以 A/B 会同时差"内容"与"预算"两件事。
**对策**：训练端加一个 `--weight-norm` 选项（把每行权重除以全局平均权重 ⇒ 平均行权重恒为 1，
只改分布不改预算，默认开）；**报告里必须打印加权后的"每类权重占比"**，否则读不出稀释程度。

**守护读数（判据的一部分）**：闪电通道的 CE 与"同格率"**不得劣化**（对照 §5）。

**3.4 ✅ 行为与监督解耦：对局仍然用 argmax 类出招。**
这一步**不改行为** ⇒ 采集到的局面分布**仍是部署分布**，**不引入分布外风险**
（这是相对"随机钉类"方案的一个实质改进：随机钉类会改变对局分布）。
⇒ `az_posnet_random_class_plan.md` 的钉类机制在本计划里**不再用于第一段**；它保留的角色只剩
"让对局走到塔态"的行为干预（而那个目的 (a) 已经用注入做过）。

**3.5 ⚠️ 代价与诚实标注**：行为不变 ⇒ 其他通道的目标是**反事实**的（"这些局面下如果改做那个
动作会怎样"），它们在对局里**从未真实发生**。这个目标可不可信，**取决于价值网在那些
post-state 上的精度**——而这恰好是 (a) 刚改善过的东西（价值网在塔态上的误差 −9%）。
**两条线在这里互相支撑**：先让价值网会判塔态，再用它给位置网的所有通道铺监督。
但要如实记住：**反事实目标的质量上限 = 价值网的精度上限**。

**3.6 归因要求（判据要干净）**：`train_value_prior.py --ckpt X` 会把 `X` 的
`class_state`/`value_state` **照抄**进输出 ⇒ 判据里的对局会被价值网污染。
所以让被比较的两个位置网**共用同一个价值网**（都用 `valnet_A2.pt`），只差"记录口径"：

- run-A：多类记录（K=5, M=32）→ `pos_state_A`
- run-B：`K=1`（= 现状的单类记录）→ `pos_state_B`

⇒ 三臂（**都是同一批对局**，因为 K/M/λ 只改"记录"不改"行为" ⇒ 样本可直接对应）：

| 臂 | 配置 | 作用 |
|---|---|---|
| **B** | `K=1, M=0` | 现状口径 |
| **A1** | `K=1, M=32` | **只差 M 封顶** ⇒ 用来把"多类"的效应与"M 封顶"分开 |
| **A** | `K=5, M=32, λ=0.2` | 主：多类记录 |

⇒ **A vs A1 隔离"多类"，A1 vs B 隔离"M 封顶"**；历史 42.2% 只当粗参照。
（A1 是冒烟后才加的：原计划只有 A/B，但那样两臂差了 K 与 M **两个**变量。）

## 4. 第二步：钉类 + 只搜位置 + visit 目标 + 单通道训练（机制已定，实施待定）

> 用户 2026-09-20 给出设计（与标准 AlphaZero 的位置分支一致），我实测后认为**可行**。
> 原先把这一步判为"受阻"（"几乎没有选择余地"）的理由**是假象**——见 §4.2 的修正。

**4.1 机制**

- 每个决策点**把类固定住**：由类网 argmax 或**随机注入钉类**决定（= 沿用
  `az_posnet_random_class_plan.md` 的钉类定义）；
- 搜索**只在"该类的位置轴"上做**（`--search-mode pos-only`，即现有配置）；
- 训练目标 = **MCTS 的 visit 分布**（不是 1-ply 价值）；
- **只训被钉的那一个通道**，其余通道用 anchor 拉住（§4.3）。

**4.1.1 钉类比例：`p = 2%`（用户 2026-09-20 指定，按头独立）**

⚠️ **但 2% 下的每通道样本量偏薄，先算清楚**：每局约 730 个决策点 ⇒ 每局 (决策点, 头) ≈ 2,188。
被钉的 (决策点, 头) = 2188 × 局数 × p，平均分到 23 个类：

| 局数 | 被钉 (决策点,头) | 平均每通道 |
|---|---|---|
| 400 | 17,508 | **~761** |
| 1000 | 43,770 | ~1,903 |
| 2000 | 87,540 | ~3,806 |

对照：闪电通道在 `vp_400` 里拿到 **8,324** 个 (决策点,头)。⇒ **p=2% + 400 局时，
其他通道各只有约 760 条（闪电的 9%）**，很可能不够训一张图。

⚠️ **为什么不能照搬 §3 的"多类记录"**：那个技巧的前提是"记录"**不花钱**（1-ply 只要价值网前向）。
而第二阶段的"记录"= **一次搜索**（256/4 约 2.4 秒）⇒ 若每回合钉全部 ~19 个合法类，
就是 **46 秒/决策点、一局要几小时**，不可行。⇒ **visit 目标只能靠采样**，
所以必然面对"钉类比例 ↔ 覆盖率"的取舍。

⇒ **结论**：第二阶段**加大局数**（M=0 口径下 400 局约 24 分钟 ⇒ 2000 局约 2 小时，可接受），
**并报告"每通道实际拿到的样本数"**；若某些通道仍太少，再用 §4.4 的轮换把名额集中到它们身上。

**4.2 ✅ 修正（用户 2026-09-20 质疑 → 受控实测）："没得选"是"类这回合不可执行"，不是"通道尖"**

我先前把"钉 17 只有 1 个候选"解释成"闪电通道很尖"——**这个归因是错的**。受控 2×2
（同一个模型，只变"买得起/买不起"与钉的类）：

| 情形 | 钉的类 | 该类合法格数 | **root 候选数** | 首选 |
|---|---|---|---|---|
| **买不起闪电**（coins 50~59 < 90）| 17 | 271 | **1** | **`()` = 强制空过** |
| **买得起且冷却好**（coins 93, cd=0）| 17 | 271 | **24** | 闪电(12,9) |
| 买得起 | 11 / 9（未训通道）| 33 | **24** | 建塔 |

**机制**：买不起 / 冷却中 ⇒ `decode_head` 返回 `None` ⇒ **每次采样都塌成同一个空 bundle**
⇒ 去重后只剩 1 个候选 ⇒ 被 `skip-single-candidate` 跳过。**与位置图的尖锐程度无关。**

**⇒ 顺带修正 `room(>=2 cands)=2.7%` 的读法**（`eval.py` 的 diag；老文档也记过同一个数字：
`az_action_factorization_idea.md`、`az_three_net_split_plan.md`）：那个 2.7%
≈ **"可执行回合"的占比**（其余回合因买不起/冷却中塌成强制空过），**不是"搜索没得选"**。

**⇒ 对第二阶段的含义（比原来乐观）**：
- **可执行的回合上搜索余地是满的，闪电也不例外（24 个候选）** ⇒ visit 目标是可用的；
- 第二阶段**不需要**为了"制造 room"去调 `t_pos`；需要的只是**钉类轮换**来覆盖其他通道
  （因为类网 argmax 几乎总是 17）。

**4.3 "只训那一个通道"：损失层面已成立，要处理的是骨干带动**

位置 CE **本来就只绑定被钉类的通道**（其余通道没有 target），所以"只训那一个通道"在损失层面
**已经自动成立**。真正会漂的是**共享骨干**——它一动，其余 23 个通道的输出就跟着变。三种锚定：

| 选项 | 做法 | 评价 |
|---|---|---|
| (a) 现状 | 参数空间 L2 锚回初值（`train_value_prior.py --anchor-lambda`）| 最便宜，但**不区分通道**，只限制整体移动量 |
| **(b)** | 只锚 `action_map_conv` 里**其他 23 个输出通道**的权重（权重形状 `(24, C, 1, 1)` ⇒ 可按通道切片）| 便宜、贴合意图；挡不住"骨干动" |
| **(c) 推荐** | **地图空间**锚定：`MSE(action_map, 记录值)` **只对非被钉通道** | = 项目**原版 anchor 的语义**（`az_train` 的 policy anchor 就是对记录输出做 trust-region）；最忠实，代价是要记全图（或同 ckpt 确定性重算）|

**4.4 ⚠️ 自限制：已从"必须设计"降级为"要监控"**

原担心是"训练会让被钉通道**变尖** ⇒ 采样集中 ⇒ room 又没了"（并把闪电的"1 个候选"当成了这个终局）。
但 §4.2 修正后：那个 1 是**买不起**造成的假象，而**已训的闪电在 `t_pos=1.0` 下仍有 24 个候选**
⇒ 现在远没到"尖到没得选"。

⇒ 因此这条降级为**监控项**：若某通道训完后候选数掉得很少，说明它**已经尖到没有可学信号**
（那时要在报告里区分"训好了"和"没数据"，见 §5）。两条对策仍留作备选：

1. **优先/轮流钉"还有 room 的通道"**（例如按候选数排序、或按"离上一次被训的回合数"轮换）；
2. **采集时把 `t_pos` 调高**保持探索性（目标仍是 visit，只是采样更散）——**倾向这个更稳**
   （它与 §1.6 的口径也不冲突：§1.6 说的是"不要把训练目标的 τ 调小去拟合噪声峰"，
   这里调的是**采样**温度）。

**4.5 ⚠️ 守卫（同 §1.7）**：钉到"买不起的超武类"时，解码器走"读通道 16 的自动降级"
⇒ 必须校验"**执行的动作确实是被钉的那个类**"，否则 visit 目标会挂到错误的通道上。
（实测旁证：钉 17 得到 1 个候选 = 买不起闪电且没有塔 ⇒ 只能空过，与这条一致。）

**4.6 与类轴的关系**：钉类让"类"由**外部**（argmax 或随机）决定 ⇒ 这一步**不训类网**，
与 `az_class_axis_plan.md` §5(b)（重训类网）**正交**。但若共用同一批数据，要把两类目标
分开归因（别让类轴的梯度混进位置轴的读数）。

**4.7 预期**：**主要增益在这一步**。依据 §1.4：搜索选点 **0.5391** > 我们的网 **0.4922**
> 1-ply 价值 **0.3281**。第一阶段买的是"别让其他通道是空白/噪声"，第二阶段才买
"比一次性选点更好"。

## 5. 预注册判据（第 1 段；跑前写死）

**主判据**（沿用文档既有口径，不新造尺子）：

1. **只换闪电落点 vs 完整 rule_v4**（`az_value_prior_plan.md` §0.3.1 那个台子）：
   新位置网 **> 0.4922** 才算赢；同时报 1-ply 价值那一栏的变化。
2. **vs rule_v4 256 局**（4 批 seed × 64，`eval.py` 同协议）：对照 ① `pos_state_B`（§3.6）
   与 ② 历史 `posnet_r1` 的 42.2%。

**守护判据（不得劣化）**：
- **闪电通道**的 CE 与"网的 argmax vs 目标 argmax 同格率"（现 39.0%）——加权/多类不应把它训坏。

**诊断读数**（不判成败，但必须打印）：
- **逐通道的 CE / top1 / 记录行数** ⇒ 确认机制真生效（应有 ~19 个通道拿到数据，而不是 1 个）
- **`cls` 直方图**（应覆盖多个类，而不是 100% 集中于 17）
- **强制格校验丢弃的行数**（§1.7；非零说明自动降级那类问题被正确挡住了）
- 实际成本倍率（行数 / 价值网前向数，对照 §3.2）
- raw 镜像（新位置网 vs 旧位置网，不搜索）
- `posnet_v2` 的 `class_state`/`value_state` 是否 = `valnet_A2` 的（逐位校验，防归因被污染）

**失败判据**：判据 1 与 2 都不涨 ⇒ "更丰富的目标"（§4，visit）是唯一出路 ⇒ 直接进第二阶段
（§4 的机制已定，见 §4.1–4.5）。

### 第 2 阶段的判据（先写在这，实施时照用）

- **主判据同 §5 判据 1/2**（同一把尺子，不新造），对照臂 = 只做第一阶段的位置网。
- **机制判据（必须报，否则不知道机制有没有生效）**：
  - **每个回合钉到的通道的 room 分布**（候选数；§4.2 说它是 1 与 24 的两极，要报直方图）
  - **各通道被钉到的次数**（应轮换覆盖，而不是全挤在闪电）
  - **被"钉类守卫"（§4.5）丢弃的行数**
  - **anchor 的实际效果**：非被钉通道的输出在训练前后的变化幅度（地图空间 L2）
- ⚠️ 若某个通道训完仍然 room=1（= 已经尖到没有选择），说明它**已经没有可学的信号了**，
  这不是失败，要在报告里区分"训好了"和"没数据"。

## 6. 风险

| 风险 | 说明 |
|---|---|
| **稀释闪电通道** | 它从 100% 的行掉到 ~1/19 ⇒ 必须按通道加权，且要盯"闪电通道不得劣化"的守护读数 |
| **反事实目标** | 行为不变 ⇒ 其他通道的目标从未真实发生；质量上限 = 价值网的精度上限（§3.5）|
| **成本 51×** | 裸做不可行；靠 M/K 压回，**先冒烟量真实倍率**再定规模（§3.2）|
| **自动降级错标** | 买不起的超武会解码成读通道 16 的降级 ⇒ 必须显式校验"顶格生效"（§1.7）|
| **§1.6 的机制** | 网的优点是"不做硬 max" ⇒ **别把 τ 调小**去拟合噪声峰 |
| **通道间干扰** | 24 个通道共享骨干 ⇒ 其他通道的梯度会流进闪电通道的特征（老风险）；多类记录会**放大**它，所以守护读数不是可选项 |
| **K/M/λ 未标定** | 三个新旋钮都没有实测；先小规模冒烟，不要一次性投 400 局 |
| **第二阶段的自限制（已降级）** | 原担心"训尖 ⇒ room 变小"；实测已训的闪电在 t_pos=1.0 下仍有 24 个候选 ⇒ 远没到；降为监控项（§4.4）|
| **anchor 挡不住骨干** | (a) 参数锚不区分通道 ⇒ 非被钉通道可能被骨干带漂；推荐 (c) 地图空间锚（§4.3）|
| **两类目标混淆** | 若与 §5(b)（类网）共用数据，类轴的梯度会混进位置轴读数 ⇒ 归因要分开（§4.6）|
| **p=2% 下样本偏薄** | 400 局时非闪电通道各约 760 条（闪电的 9%）⇒ 第二阶段要加大局数或轮换集中名额（§4.1.1）|

## 7. 与现有文档的关系

- `az_posnet_random_class_plan.md`（2026-09-16）：钉类机制的原始定义。**本计划的第一段不再用它**
  （§3.4）；它的"钉类"只在我们想要"让对局走到塔态"的行为干预时才有用，而那个目的 (a) 已达成。
- `az_class_axis_plan.md` §5(c)：把随机钉类定位成"打破 bootstrap 陷阱的强制探索"——本计划
  **用"多类记录"替代它的第一段角色**（铺监督），而"(c) 让通道拿到监督"这个目的被更直接地达成。
  §5(b)（重训类网）与本文 §4 **正交**（§4.6：钉类让"类"由外部决定 ⇒ 不训类网），
  但若共用数据要把两类目标分开归因。
- `az_value_prior_plan.md` §0.3.1：给出"目标要更深"的证据 ⇒ 本计划的 §4。
- `docs/az_t_pos_default_fix.md`：`t_pos` 默认值口径（本计划沿用它）。

## 8. 实施顺序

1. `collect_value_prior.py` 加多类记录（`--multi-class-k K`、`--max-cells M`、`--row-weight-lambda λ`）
   + **强制格校验**（§1.7）；**默认行为完全不变**（K=1 时逐位等价于现状）
2. **冒烟**：`K=5, M=32` 跑 2~4 局，核对 ① 实际成本倍率、② `cls` 覆盖的类数、
   ③ 强制格校验丢弃的行数、④ 闪电通道的行数占比
3. **run-A**（多类）与 **run-B**（`K=1`，= 现状）各 400 局，`--ckpt training_history/inject_ex02/valnet_A2.pt`
4. `train_value_prior.py` 各训一个 ⇒ `posnet_A.pt` / `posnet_B.pt`（顺带换 `--seed` 复训一次）
5. 判据 1（闪电落点台子）+ 判据 2（vs rule_v4 256 局，A 与 B 同协议）+ 守护读数
6. 记档：逐通道 CE、`cls` 直方图、丢弃行数、判据 1/2
7. **（第二阶段）** 在第一步的结果上再走一遍：`collect_value_prior.py` 的**搜索版**（钉类 +
   `pos-only` 搜索 + 记 visit）、`train_value_prior.py` 加地图空间 anchor（§4.3(c)）+ 单通道 loss；
   先冒烟核对 §4.2 的 room 两极、§4.4 的轮换/`t_pos`、§4.5 的守卫丢弃数
