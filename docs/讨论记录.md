# 蚁洋陷役2 — AI 策略讨论记录

## 基本信息

- **项目**: 2025-2026 第三十届智能体大赛 蚁洋陷役2
- **参赛者**: 
- **创建日期**: 2026-06-29

---

## 讨论记录

### 2026-06-29：神经网络输出方案讨论

**议题**：动作空间极大，如何设计神经网络输出方式

**方案1**：多分支输出头（操作类型、位置、塔ID等分别输出）× 复制3-5份，解码成组合动作
**方案2**：动作评分法 — 用 ActionCatalog 生成候选 bundle，网络对每个 bundle 打分，选最高分

**结论**：采用方案2（动作评分法，后改为空间图方案）

### 2026-06-29：模型输入方案确认

官方 `FeatureExtractor` 提供完整特征：board(28,19,19) + stats(~22→42维)

### 2026-06-29：Encoder 架构 — Conv7×7 + ResBlock×6 (64ch)，无池化

### 2026-06-29：输出方案 — 共享空间图(23×19×19) + 3×分类头(23维)

### 2026-06-29：训练方案 — 进化策略（ES），以胜率为 fitness

### 2026-06-30：AI 操作合法性保证

- 解码器用 `can_apply_operation(op, pending)` 逐条校验，同玩家内组合操作保证合法
- 跨玩家冲突无法避免（双方同时决策后引擎串行执行），`cold_handle_rule_illegal=True` 正确处理
- 高台区域各玩家不重叠（HIGHLAND_CELLS[0] != HIGHLAND_CELLS[1]），建塔不会跨玩家冲突
- 引擎层 `resolve_turn` → `apply_operation_list` 做最终兜底

### 2026-06-30：性能分析 & PyTorch 多进程调优

**性能瓶颈拆解**（单进程 Neural vs Example）：
- FeatureExtractor × 2: 5.6ms (47% of turn time)
- 网络前向 × 2: 4.0ms (33%)
- 引擎 resolve_turn: 2.7ms (22%)
- Decoder × 2: 2.0ms (17%)

**多进程加速瓶颈**：
- Windows spawn 模式下每个 worker 需独立 import PyTorch（~2-3s）
- PyTorch MKL/OpenMP 默认使用所有 CPU 核 → 12 进程 × 8 线程 = 96 线程争抢 → 严重上下文切换
- **修复**：worker 函数开头设 `torch.set_num_threads(1)` + `OMP_NUM_THREADS=1`
- 效果：Neural vs Neural 24 局从 45.5s → 11.0s（4.1x），Neural vs Example 从 179.9s → 41.8s（4.3x）

**多进程吞吐对比**（12 workers, 修复后, Neural vs Neural）：
- 24 局 11.0s, 459ms/局, 2.2局/s
- 自对弈比 fixed opponent 快约 4-6 倍，建议训练用自对弈

### 2026-06-30：训练程序改进

- **Ctrl+C 优雅终止**：`signal.signal` 捕获 SIGINT，完成当前代后保存 `interrupt_gen_NNNN.pt`
- 第二次 Ctrl+C 强制退出
- **Pool 跨代复用**：移至 main loop 中创建，避免每代重建进程
- **训练历史**：按时间戳目录保存 config/log/CSV/模型权重

### 2026-06-30：自对弈（Self-Play）改造

- 弃用固定对手（example/random），改为种群内随机选对手
- 新增 `select_opponents()` 函数 — 对手选择与评估逻辑解耦，便于未来扩展
  - 当前策略：个体 i 的每局对手 = 随机选 j ≠ i
  - 后续可扩展：精英池、历史对手池、锦标赛选择等
- `_eval_worker(params, opp_params, seed)` 双方都是 NeuralAgent
- 效果：自对弈比 vs Example 快 3.7x（pop=6, workers=6 时 7.5s/代 vs 28s/代）

### 2026-06-30：ES 算法正确性验证

- 在 553K 维参数空间上做 L2 收敛测试失败 → 维度诅咒导致梯度信噪比太低
- 改为只在前 1000 维计算距离 → 距离从 9.26 稳定下降到 5.51（1.7x），确认 ES 算法正确
- 高维收敛慢是预期行为，不是 bug。游戏 fitness landscape 有结构，不一定需要完美收敛

### 2026-06-30：Ctrl+C 修复 & PAUSE 暂停功能

- Windows 上信号无法中断 `t.get()` 阻塞调用
- 改为 `pool.starmap_async()` + 2 秒超时轮询中断标志
- 信号处理器设置 `pool._interrupted` 标志位，触发 step() 提前返回
- 新增 PAUSE 文件暂停机制：创建 `training_history_xxx/PAUSE` 暂停，删除恢复

### 2026-06-30：Logger 统一时间戳 & 个体详情输出

- `Logger.print_table()` 改为每行带时间戳前缀
- 每代输出 `ind_dist` — fitness 分布直方图（0.00 到 1.00 五桶）
- 每代输出最优个体的先手/后手胜率详情

### 2026-06-30：对手选择优化 — 固定配对 + 先手后手均衡

- 之前：每个个体独立抽对手，有放回抽样，先后手随机
- 现在：
  - 无放回抽 `games/2` 个对手（对手池每代固定）
  - 每个对手打 2 局：seed 偶→先手，seed 奇→后手
  - 确保每个个体的先手/后手局数严格 1:1（games 必须为偶数）
- 梯度信噪比提升方向：后续可加精英偏置、跨世代池等

### 2026-06-30：Mirrored Sampling + sigma 调大 + 动量优化器

- **Mirrored Sampling**：
  - 每个噪声 ε 产生两个个体：θ+σ·ε 和 θ-σ·ε
  - 梯度公式：g = Σ[(f⁺-f⁻)·ε] / (N·σ)，方差减半，等效 pop_size ×2
  - 要求 `--pop-size` 为偶数
- **sigma 默认值 0.05 → 0.2**：更大的扰动让个体间 fitness 差异显著，rank shaping 不再随机
- **动量优化器（momentum=0.9 默认）**：`v = μ·v + lr·g`，跨代平滑梯度的噪声累积
- **Histogram 自适应桶数**：从 [8, 6, 5, 4] 选能整除 games 的最大值做分桶数
- 综合效果：梯度信号信噪比提升约 4 倍，动量进一步平滑更新方向

### 2026-06-30：Single Head 训练模式

- 问题：3 个策略头在随机初始化后 argmax 各自锁死在不同的动作类上（建塔 vs 降级），互相抵消，浪费所有金币
- 解决方案：新增 `--single-head` 选项，只保留 head1 的参数
  - `AntWarNetwork.__init__(single_head=True)` 跳过 head2/head3 的创建
  - 参数量从 553,053 → 550,063（减少 ~3K）
  - `decode_network_output` 动态检测存在的 head key
- 训练效果：单头选出唯一的动作类，消除多头冲突
- 训练完成后可用 `expand_to_3heads.py` 将 head1 权重复制到 head2/head3，恢复 3 动作/回合

### 2026-06-30：行为克隆（Behavior Cloning）方案

- **现状**：单头 ES（pop=54, games=8, 550K 参数）训练 11 代后，网络仍然输出 BUILD→DOWNGRADE 循环，ES 在高维下的梯度信噪比太低，无法学到稳定策略
- **方案**：收集 ExampleAI 自对弈数据，监督学习网络的动作类预测头
  - 数据：每回合录 board(28,19,19) + stats(42) + ExampleAI 选的动作类(0-22)
  - 位置头不动：action_map 冻结（随机初始化），解码器 argmax 选位置，至少合法
  - 训练目标：只训练 class head，CrossEntropyLoss 监督预测类
  - 预期：几百局（~10 万条数据），class 准确率从 4% 提升到 80%+
  - 后续：BC 冷启动后用 ES 微调位置头

### 2026-06-30：增加 HOLD 类（class 23）

- **问题**：网络没有"什么都不做"的类，导致 argmax 被迫选操作，ES 中模型只能通过 BUILD→DOWNGRADE 循环"合法空转"
- **改动**：`NUM_CLASSES` 23→24，新增 class 23 = HOLD
  - `make_class_mask`: HOLD 永远合法
  - `decode_head`: class 23 → return None（空操作）
  - `collect_data.py`: ExampleAI 选空操作 bundle 时记录为 class 23
- **效果**：3 代 ES 对 ExampleAI 胜率从 4% → 42%
- **退化问题**：15+ 代后再次降到 0%——模型学会"建几个塔→HOLD"，自对弈中双方都 HOLD 导致 fitness 信号无效
- **交叉验证**：gen_15 对 gen_10 胜率 57%，说明模型在优化针对自对弈对手的策略，不是 ExampleAI
- **结论**：HOLD 类修好了核心问题，自对弈对手选择需要进一步优化

### 2026-06-30：跨世代精英池对手选择

- **问题**：自对弈对手全部来自当前种群 → 策略逐渐退化为"互相 HOLD"，对 ExampleAI 胜率从 42% 跌回 0%
- **方案**：每代结束后将当前种群的 mean 和 top1 参数加入精英池
  - 对手从精英池中随机选取（按世代从新到旧）
  - 种群内个体 vs 精英对手，防止策略退化
  - 精英池保留最近 20 代（40 个对手向量）
- **效果**：对手池包含 gen_3-11（对 ExampleAI 胜率 42-48%）的策略，梯度不会全往 HOLD 方向推
- **代码改动**：`es_train.py` — `ESTrainer.elite_pool` 列表 + `step()` 中从池中选择对手

### 2026-06-30：精英池持久化 + 加权选择

- **持久化**：精英池保存到 checkpoint 中，中断恢复后不会丢失
  - `save_checkpoint()` 和 `load_checkpoint()` 新增 `elite_pool` 字段
- **加权选择**：较新的世代作为对手的概率更高
  - 权重 = 0.85 ^ 代距（当前代 1.0，5 代前 0.44，20 代前 0.04）
  - 防止早期随机世代占据过多对手配额

---

## 决策日志

| 日期 | 决策内容 | 备注 |
|------|---------|------|
| 2026-06-29 | 输出采用"共享空间图 + 3份分类头"方案 → 23个动作类 | Producer/基地升级拆为独立通道 |
| 2026-06-29 | Encoder：首层 Conv7×7 + 后续 ResBlock×6~10 Conv3×3 | 无池化，GAP 汇聚 |
| 2026-06-29 | 训练方案：进化策略（ES） | 避免 RL 奖励设计问题 |
| 2026-06-29 | 模型输入：官方 FeatureExtractor encode_observation | board(28,19,19) + stats(42维) |
| 2026-06-30 | ES 训练对手 | 建议自对弈（Neural vs Neural），比固定对手快 4-6x |
| 2026-06-30 | 多进程调优 | worker 内 `torch.set_num_threads(1)` 防 MKL 线程争抢 |
| 2026-06-30 | Ctrl+C 优雅终止 | 信号处理器 + pool.terminate + 中断保存 checkpoint |
| 2026-06-30 | 自对弈训练 | `select_opponents()` 解耦对手选择，种群内随机匹配 |
| 2026-06-30 | ES 正确性验证 | 1000 子维 L2 收敛 1.7x，高维收敛慢是预期 |
| 2026-06-30 | Ctrl+C 修复 | `starmap_async` + 2s 超时轮询替代信号中断 |
| 2026-06-30 | PAUSE 文件暂停 | 创建/删除 PAUSE 文件控制训练暂停继续 |
| 2026-06-30 | Logger 时间戳 | `print_table()` 统一加时间，ES 程序去除时间管理 |
| 2026-06-30 | 个体详情输出 | 每代 fitness 分布直方图 + 最优个体先手/后手胜率 |
| 2026-06-30 | 对手选择 | 无放回 + 先手后手均衡（games/2 个对手各打两局） |
| 2026-06-30 | Mirrored Sampling + sigma 0.2 | 每个噪声 +σ/-σ 双个体，梯度方差减半 |
| 2026-06-30 | 动量优化器 | `v=μ·v+lr·g`，momentum=0.9，平滑梯度噪声 |
| 2026-06-30 | Histogram 自适应 | 从 [8,6,5,4] 选最大整除 games 的桶数 |
| 2026-06-30 | Single Head 训练 | `--single-head` 消除多头冲突，`expand_to_3heads.py` 恢复 |
| 2026-06-30 | Top-2 参数保存 | checkpoint 文件保存最优个体参数 `top2_params` |
| 2026-06-30 | 动量持久化 | checkpoint 保存 `velocity`，中断恢复不丢动量 |
| 2026-06-30 | 行为克隆（BC） | ExampleAI 自对弈数据 → 监督学习 class head，位置头冻结 |
| 2026-06-30 | HOLD 类 (class 23) | `NUM_CLASSES` 23→24，消除 BUILD→DOWNGRADE 循环 |
| 2026-06-30 | 自对弈对手选择问题 | gen_15 > gen_10 自对弈但 vs ExampleAI = 0%，需混入 ExampleAI 对手 |
| 2026-06-30 | 跨世代精英池 | 对手从历史 gen 的 mean/top1 中选，保留 20 代，防止 HOLD 退化 |
| 2026-06-30 | 精英池持久化 + 加权 | checkpoint 保存 elite_pool，按 0.85^代距 加权选对手 |
