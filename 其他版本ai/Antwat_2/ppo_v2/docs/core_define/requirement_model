
TowerDamageHead — 单回合原始值
- 初始值 = 回合开始前每座塔的 HP
- 操作变化值 = build(+hp) / upgrade(+hp) / downgrade(−hp) 造成的 HP 变化量
- 结束值 = advance_round() 后每座塔的 HP
- 单回合原始值 = −(结束值 − 操作变化值 − 初始值) = 初始值 + 操作变化值 − 结束值
- 含义： advance_round() 中敌方蚂蚁造成的 HP 损失。正数表示伤害
GoldIncomeHead — 单回合原始值
- 初始值 = 回合开始前的金币余额
- 操作变化值 = 操作花费（建塔/升级/武器，为负数）
- 结束值 = advance_round() 后的金币余额
- 单回合原始值 = 结束值 − 操作变化值 − 初始值
- 含义： advance_round() 中塔/基地产生的毛收入。正数表示收入
聚合
- 两个 Head 的聚合方式相同：对时间步 t 和视界 h，将接下来 h 个回合的单回合原始值累加。